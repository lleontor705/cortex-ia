# Agent Reference

cortex-ia supports exactly four agents: Claude Code (`claude-code`), OpenCode
(`opencode`), VS Code Copilot (`vscode-copilot`), and Codex (`codex`).

See [agents.md](agents.md) for adapter paths, capabilities, and MCP strategy
details.
