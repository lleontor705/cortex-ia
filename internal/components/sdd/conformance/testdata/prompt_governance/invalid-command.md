---
agent: orchestrator
---

WORKFLOW: query the board, launch sub-agents, and run the complete pipeline. This command is intentionally too short and contains orchestration policy instead of only activation, context capture, and executable dispatch.
