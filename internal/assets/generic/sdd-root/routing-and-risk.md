# Routing and Risk

Route by risk, reversibility, effects, evidence. Fast-track requires proof;
override deepens. Missing evidence blocks.

Gauntlet c > b > a: read_only; no_side_effect; evidence_missing;
conflicting_facts; reason code. Stages: constitutional preflight; requirement quality; blueprint handoff; deterministic gates; bounded remediation.

Phases: init, explore, propose, spec, design, tasks, apply, verify, archive. Reject
a tenth phase, new WorkflowIR phase, and ears parser. A minion is an alias;
orchestrator alone routes; reference-only, sequential by default, disjoint read-only.

Delivery: tests, fixtures, and assertions; at most two; no production. Owner:
skill or prompt-equivalent. Documented degradation: native, sequential,
prepare, apply, rollback, idempotent, unmanaged. Add no new role, no adapter api,
no scheduler, no permission expansion, no runtime orchestration.
