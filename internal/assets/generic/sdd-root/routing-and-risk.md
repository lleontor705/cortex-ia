# Routing and Risk

Risk, reversibility, trust, migration, effects, tests, override: fast-track;
missing evidence: blocked.

Gauntlet: c > b > a; read_only; no_side_effect; evidence_missing;
conflicting_facts; reason code. Stages: constitutional preflight; requirement quality; blueprint handoff; deterministic gates; bounded remediation. Preserve init, explore, propose, spec, design, tasks, apply, verify, archive; reject a tenth phase, new WorkflowIR phase, and ears parser. Minion aliases remain aliases; orchestrator alone owns authority; reference-only; sequential by default; disjoint read-only.

Delivery: tests, fixtures, and assertions; at most two; no production; skill; prompt-equivalent; documented degradation; native; sequential; prepare; apply; rollback; idempotent; unmanaged. No new role; no adapter api; no scheduler; no permission expansion; no runtime orchestration.
