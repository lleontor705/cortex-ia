---
description: Run a bounded Fast-TDD loop when a reliable oracle exists
agent: orchestrator
subtask: false
---

Evaluate Fast-TDD eligibility, then route through `fast-tdd`; if its safety gate fails, explain why and propose the closest safe workflow: $ARGUMENTS
