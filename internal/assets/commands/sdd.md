---
description: Plan and execute a right-sized SDD Lite or Full change
agent: orchestrator
subtask: false
---

Evaluate SDD depth, then route this change through `sdd-lite` or `sdd-full`. Persist only validated ForgeSpec contracts and dispatch ready tasks directly to leaf minions: $ARGUMENTS
