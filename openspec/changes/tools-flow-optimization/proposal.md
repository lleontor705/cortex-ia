# Tools flow optimization

Implement the seven authorized improvements through six cohesive slices: durable submission evidence; artifact write authority; automatic live-controller renewal and batch release; integrated hashes; coherent job queries; and bounded snapshot streaming. Preserve the current dirty baseline, existing public aliases, independent review and AGY account authentication with temporary homes, configured model and explicit SkipPermissions true.

Use SDD Full because public contracts and persisted authority evidence change. No generic action-bag API, broader database rewrite, Cortex upstream modification, production delegation, remote deployment, or automatic fallback after job acceptance. Meaningful Cortex relationships remain supported; only unconditional prompt ceremony is removed.
