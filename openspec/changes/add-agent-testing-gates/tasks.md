# Tasks — Agent testing-quality gates (Wave 1)

Board: `agent-testing-quality` | Workflow: sdd-lite | Workload: flexible | spec_plane: openspec | Change: add-agent-testing-gates
DAG: t1-contracts-core blocks t2-implement-loop, t3-reviewer-side, t4-planner-traceability (parallel, disjoint files). All edits are markdown go:embed assets; zero Go code this wave.

Execution note for all tasks: Cortex MCP saves are currently failing (infra); treat cortex_save as best-effort and never block on it. Final validation is `go build ./...` plus consistency greps; no Go test additions are authorized this wave.

### Task: t1-contracts-core Bake mutation-evidence gates into the shared protocol and AGENTS.md

- Requirements: REQ-ATG-001, REQ-ATG-002, REQ-ATG-004
- Depends: none
- Allowed files: internal/assets/skills/_shared/cortex-work-protocol.md, internal/assets/AGENTS.md
- Objective: Place the single normative mutation-evidence MUST in the canonical work protocol: extend §4 item 5 (run focused checks, then proportional regression) with the clause requiring fast-TDD-eligible code tasks to produce mutation evidence before `transition in_review`, and extend §8 evidence composition with the receipt obligation and the reviewer perpetually-green duty. Scope the MUST to fast-TDD-eligible code tasks with the exemption list mirroring skills/implement/SKILL.md:21 (documentation, declarative configuration, generated output, no reliable fast oracle). Mirror in AGENTS.md as cross-references only: a "Mutation Evidence Gate" item in §2 Pragmatic Invariants, a bullet under §3 Pre-Transition Workload Preflight, and upgrade the Phase-4 diagram caption (line 201) to a normative cross-reference to the protocol section.
- Acceptance: Given the post-change protocol file WHEN its §4 and §8 sections are read THEN exactly one normative mutation-evidence clause is present with KILLED, SURVIVED, and STATIC-ANALYSIS semantics and the exemption list. Given AGENTS.md WHEN §2, §3, and the Phase-4 diagram are audited THEN a Mutation Evidence Gate invariant item, a preflight bullet, and the upgraded caption all cross-reference the canonical protocol without restating normative wording. Given the anchor tokens WHEN raw greps run THEN "mutation evidence", "STATIC-ANALYSIS", and "perpetually-green" appear in cortex-work-protocol.md and "Mutation Evidence Gate" appears in AGENTS.md.
- Verification: grep -q "mutation evidence" internal/assets/skills/_shared/cortex-work-protocol.md && grep -q "STATIC-ANALYSIS" internal/assets/skills/_shared/cortex-work-protocol.md && grep -q "perpetually-green" internal/assets/skills/_shared/cortex-work-protocol.md && grep -q "Mutation Evidence Gate" internal/assets/AGENTS.md && go build ./...

### Task: t2-implement-loop Operationalize mutation evidence in the implementation loop

- Requirements: REQ-ATG-001, REQ-ATG-002, REQ-ATG-005
- Depends: t1-contracts-core
- Allowed files: internal/assets/skills/fast-tdd/SKILL.md, internal/assets/skills/implement/SKILL.md, internal/assets/agents/implement.md
- Objective: Turn the protocol gate into operative loop steps. fast-tdd: add a loop step running 1–2 reversible mutations from the mutation-testing skill after GREEN and before transition, and extend the receipt JSON with a mutation field whose accepted values are KILLED and STATIC-ANALYSIS (SURVIVED never ships; strengthen and re-run instead). implement: in steps 4 and 6 add the oracle-strength mandate (every new or changed test demonstrates failure capability by asserting a boundary, a return payload, or a failure branch), the pre-transition mutation evidence requirement, and the coverage-delta advisory (before/after go test -coverprofile MAY be reported, never gates). agents/implement.md Step 5: align wording to the skills via cross-reference, no forked normative text.
- Acceptance: Given the fast-tdd receipt schema WHEN the output JSON block is read THEN it contains a mutation field with KILLED / STATIC-ANALYSIS value semantics and a rule that a missing field on an eligible task is INCONCLUSIVE, never PASS. Given implement SKILL.md WHEN steps 4/6 are read THEN failure capability, pre-transition mutation evidence, and the coverage advisory are each present. Given agents/implement.md WHEN Step 5 is read THEN its verification wording cites the skills as the source of detail. Given anchor tokens WHEN greps run THEN "mutation" and "STATIC-ANALYSIS" appear in fast-tdd SKILL.md and "failure capability" appears in implement SKILL.md.
- Verification: grep -q "mutation" internal/assets/skills/fast-tdd/SKILL.md && grep -q "STATIC-ANALYSIS" internal/assets/skills/fast-tdd/SKILL.md && grep -q "failure capability" internal/assets/skills/implement/SKILL.md && go build ./... && go test ./internal/pipeline/ -run TestInstall_DryRun -count=1

### Task: t3-reviewer-side Define the test-strength lens in the adversary skill and reviewer agent

- Requirements: REQ-ATG-003, REQ-ATG-004
- Depends: t1-contracts-core
- Allowed files: internal/assets/skills/mutation-testing/SKILL.md, internal/assets/skills/code-review-adversary/SKILL.md, internal/assets/agents/reviewer.md
- Objective: Close the reachability gap. mutation-testing SKILL.md: state the invocation contract — mutation execution is implementer-side under live leases before transition (reviewer stays read-only), define the receipt field semantics (KILLED, SURVIVED never ships, STATIC-ANALYSIS minimum bar = Static Assertion Analysis of error boundaries, return payloads, failure branches), and replace the unconditional SKIPPED_UNSUPPORTED fallback with the mandatory STATIC-ANALYSIS record. code-review-adversary SKILL.md: write once the Phase 3 test-strength lens including the perpetually-green test definition (a test that would pass under any behavior change is an oracle gap → FAIL under Lens 1) and independent verification of claimed mutation evidence against diff and test source. agents/reviewer.md: add the lens pointer to the skill in Phase 3/4, the independent-evidence verification duty, and the citation making the skill the elaborated single source for the 5-phase/3-lens pipeline; do not fork normative wording.
- Acceptance: Given the adversary skill WHEN Lens 1 / Phase 3 text is read THEN the perpetually-green lens is defined there exactly once. Given agents/reviewer.md WHEN Phase 3/4 are read THEN they point to code-review-adversary SKILL.md for the lens wording and require independent verification of mutation evidence. Given mutation-testing SKILL.md WHEN the fallback section is read THEN STATIC-ANALYSIS is defined as a required field with the three-part assertion bar. Given anchor tokens WHEN greps run THEN "perpetually-green" appears in both the skill and reviewer.md and "STATIC-ANALYSIS" appears in mutation-testing SKILL.md.
- Verification: grep -q "perpetually-green" internal/assets/skills/code-review-adversary/SKILL.md && grep -q "perpetually-green" internal/assets/agents/reviewer.md && grep -q "code-review-adversary" internal/assets/agents/reviewer.md && grep -q "STATIC-ANALYSIS" internal/assets/skills/mutation-testing/SKILL.md && go build ./...

### Task: t4-planner-traceability Encode TestREQ naming and update the workflow map

- Requirements: REQ-ATG-004, REQ-ATG-005
- Depends: t1-contracts-core
- Allowed files: internal/assets/skills/planner/SKILL.md, internal/assets/skills/_shared/cortex-convention.md, internal/assets/skills/_shared/workflow-map.md
- Objective: Bake traceability and reflect the new gates. planner SKILL.md: require that verification commands for REQ-bound tasks targeting persistent tests use the TestREQ_{DOMAIN}_{NNN}_<slug> name (`-run TestREQ_...`), with language-conditional adaptation allowed. cortex-convention.md: extend the strict traceability chain (Requirements → Scenarios → Design → Acceptance/Oracles → Non-goals → Tasks) with the naming convention and a per-REQ Test: oracle line requirement. workflow-map.md: update the fast-tdd and review rows so the phase contract mentions mutation evidence, and add the structural-validation note for TestREQ naming. Convention-level only: no Go validator change this wave.
- Acceptance: Given planner SKILL.md WHEN the task-field guidance is read THEN TestREQ naming and -run targeting are specified. Given cortex-convention.md WHEN the traceability section is read THEN the naming convention appears in the chain. Given workflow-map.md WHEN the fast-tdd/review rows are read THEN mutation evidence is reflected and the TestREQ structural-validation note is present. Given anchor tokens WHEN greps run THEN "TestREQ_" appears in planner SKILL.md and cortex-convention.md and "mutation" appears in workflow-map.md.
- Verification: grep -q "TestREQ_" internal/assets/skills/planner/SKILL.md && grep -q "TestREQ_" internal/assets/skills/_shared/cortex-convention.md && grep -q "mutation" internal/assets/skills/_shared/workflow-map.md && go build ./...
