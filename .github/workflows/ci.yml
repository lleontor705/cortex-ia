name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

permissions:
  contents: read

jobs:
  quality:
    uses: lleontor705/ats-deploy-public/.github/workflows/quality-go.yml@main
    with:
      go-version: "1.26"

  security:
    uses: lleontor705/ats-deploy-public/.github/workflows/security-scan.yml@main
